```astro
---
const projects = [
  {
    title: "Záhrada pri vile",
    location: "Bratislava, Slovensko",
    year: "2025",
    image:
      "https://images.unsplash.com/photo-1558521958-0a228e77e984?auto=format&fit=crop&w=1600&q=85",
  },
  {
    title: "Záhrada mestského domu",
    location: "Praha, Česká republika",
    year: "2024",
    image:
      "https://images.unsplash.com/photo-1585320806297-9794b3e4eeae?auto=format&fit=crop&w=1600&q=85",
  },
  {
    title: "Útočisko v lese",
    location: "Tatry, Slovensko",
    year: "2024",
    image:
      "https://images.unsplash.com/photo-1584738766473-61c083514bf4?auto=format&fit=crop&w=1600&q=85",
  },
  {
    title: "Mestská záhrada",
    location: "Brno, Česká republika",
    year: "2023",
    image:
      "https://images.unsplash.com/photo-1598902108854-10e335adac99?auto=format&fit=crop&w=1600&q=85",
  },
];
---

<!doctype html>
<html lang="sk">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta
      name="description"
      content="HORTUS VIRIDIS — ateliér záhradnej a krajinnej architektúry. Individuálny prístup, zaujímavé riešenia, funkčnosť a precíznosť."
    />
    <title>HORTUS VIRIDIS — Záhradná architektúra</title>
  </head>

  <body>
    <header class="header">
      <a href="/" class="logo">HORTUS VIRIDIS<span>.</span></a>

      <nav>
        <a href="#projects">Projekty</a>
        <a href="#o-nas">O nás</a>
        <a href="#contact">Kontakt</a>
      </nav>

      <button class="menu" aria-label="Otvoriť menu">
        <span></span>
        <span></span>
      </button>
    </header>

    <main>
      <!-- HERO -->
      <section class="hero">
        <div class="hero-image">
          <img
            src="/images/title.jpg"
            alt="Minimalistická záhrada"
          />
        </div>

        <div class="hero-content">
          <p class="eyebrow">Záhradná architektúra · Od roku 2015</p>

          <h1>
            Hortus Viridis<br />
            záhradní architekti.
          </h1>

          <div class="hero-bottom">
            <p class="eyebrow">Záhradná architektúra · Od roku 2015</p>

            <a
              href="#projects"
              class="circle-link"
              aria-label="Zobraziť projekty"
            >
              <span>↓</span>
            </a>
          </div>
        </div>
      </section>

      <!-- O NÁS -->
      <section class="intro" id="o-nas">
        <div class="section-number">01</div>

        <div class="intro-content">
          <p class="eyebrow">O nás</p>

          <h2>
            Tvoríme záhrady,<br />
            ktoré prinášajú<br />
            <em>radosť.</em>
          </h2>

          <div class="intro-text">
            <p>
              Sme mladá dvojica záhradných architektov, ktorá sa po 5 ročnom
              pôsobení v praxi rozhodla založiť ateliér záhradnej a krajinnej
              architektúry – <strong>HORTUS VIRIDIS.</strong>
            </p>

            <p>
              Prinášame Vám individuálny prístup, zaujímavé riešenia,
              funkčnosť, precíznosť a veľa radosti z Vašej novej záhrady.
            </p>

            <a href="#contact" class="text-link">
              Kontaktujte nás
              <span>↗</span>
            </a>
          </div>
        </div>
      </section>

      <!-- SLUŽBY -->
      <section class="services">
        <div class="section-number">02</div>

        <div class="services-content">
          <p class="eyebrow">Naše služby</p>

          <div class="service-list">
            <div class="service">
              <span>01</span>

              <h3>Štúdia</h3>

              <p>
                Kompozičné riešenie záhrady.
              </p>
            </div>

            <div class="service">
              <span>02</span>

              <h3>Projektová dokumentácia</h3>

              <p>
                Kompletná projektová dokumentácia (PD).
              </p>
            </div>

            <div class="service">
              <span>03</span>

              <h3>Autorský dozor</h3>

              <p>
                Dohľad nad realizáciou a dodržaním navrhnutého riešenia.
              </p>
            </div>

            <div class="service">
              <span>04</span>

              <h3>Sadové úpravy</h3>

              <p>
                Realizácia sadových úprav podľa projektovej dokumentácie.
              </p>
            </div>

            <div class="service">
              <span>05</span>

              <h3>Závlahové systémy</h3>

              <p>
                Návrh a realizácia závlahových systémov.
              </p>
            </div>

            <div class="service">
              <span>06</span>

              <h3>Floristika a aranžovanie</h3>

              <p>
                Floristika, aranžovanie a tvorba kvetinových kompozícií.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- PROJECTS -->
      <section class="projects" id="projects">
        <div class="section-heading">
          <div>
            <p class="eyebrow">Vybrané realizácie</p>
            <h2>Projekty</h2>
          </div>

          <span class="section-number">03</span>
        </div>

        <div class="project-grid">
          {
            projects.map((project, index) => (
              <a
                href={`/projekty/${project.title.toLowerCase().replaceAll(" ", "-")}`}
                class={`project project-${index + 1}`}
              >
                <div class="project-image">
                  <img
                    src={project.image}
                    alt={project.title}
                    loading="lazy"
                  />
                  <span class="project-arrow">↗</span>
                </div>

                <div class="project-meta">
                  <div>
                    <h3>{project.title}</h3>
                    <p>{project.location}</p>
                  </div>

                  <span>{project.year}</span>
                </div>
              </a>
            ))
          }
        </div>

        <div class="projects-footer">
          <a href="/projekty" class="outline-button">
            Všetky projekty
            <span>↗</span>
          </a>
        </div>
      </section>

      <!-- CONTACT -->
      <section class="contact" id="contact">
        <p class="eyebrow">Začnime projekt</p>

        <h2>
          Máte miesto,<br />
          ktoré čaká na zmenu?
        </h2>

        <a href="mailto:hello@atelier.example" class="contact-link">
          hello@atelier.example
          <span>↗</span>
        </a>

        <div class="contact-bottom">
          <span>Bratislava · Praha · Viedeň</span>
          <span>© 2026 HORTUS VIRIDIS</span>
        </div>
      </section>
    </main>

    <footer>
      <a href="/" class="logo">HORTUS VIRIDIS<span>.</span></a>

      <p>Záhradná architektúra</p>

      <a href="#top">Späť hore ↑</a>
    </footer>
  </body>
</html>

<style>
  :global(*) {
    box-sizing: border-box;
  }

  :global(html) {
    scroll-behavior: smooth;
  }

  :global(body) {
    margin: 0;
    background: #f3f1ec;
    color: #1c1c1a;
    font-family:
      Arial,
      Helvetica,
      sans-serif;
    font-weight: 400;
  }

  :global(a) {
    color: inherit;
    text-decoration: none;
  }

  .header {
    position: absolute;
    z-index: 10;
    top: 0;
    left: 0;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 28px 4vw;
    color: white;
    mix-blend-mode: normal;
  }

  .logo {
    font-size: 18px;
    font-weight: 600;
    letter-spacing: -0.04em;
  }

  .logo span {
    opacity: 0.6;
  }

  nav {
    display: flex;
    gap: 38px;
    font-size: 12px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  nav a {
    position: relative;
  }

  nav a::after {
    content: "";
    position: absolute;
    bottom: -6px;
    left: 0;
    width: 0;
    height: 1px;
    background: currentColor;
    transition: width 0.3s ease;
  }

  nav a:hover::after {
    width: 100%;
  }

  .menu {
    display: none;
    border: 0;
    background: none;
  }

  .hero {
    position: relative;
    height: 100svh;
    min-height: 680px;
    color: white;
    overflow: hidden;
  }

  .hero-image {
    position: absolute;
    inset: 0;
    background: #333;
  }

  .hero-image::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.32) 0%,
      rgba(0, 0, 0, 0.02) 45%,
      rgba(0, 0, 0, 0.45) 100%
    );
  }

  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    animation: heroZoom 12s ease-out both;
  }

  @keyframes heroZoom {
    from {
      transform: scale(1.06);
    }

    to {
      transform: scale(1);
    }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 150px 4vw 45px;
  }

  .eyebrow {
    margin: 0;
    font-size: 10px;
    line-height: 1.4;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h1,
  h2,
  h3,
  p {
    margin-top: 0;
  }

  h1 {
    margin-bottom: 0;
    max-width: 1100px;
    font-size: clamp(64px, 9.2vw, 150px);
    line-height: 0.87;
    letter-spacing: -0.065em;
    font-weight: 400;
  }

  h1 em,
  h2 em {
    font-family: Georgia, "Times New Roman", serif;
    font-weight: 400;
  }

  .hero-bottom {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
  }

  .hero-bottom p {
    width: 310px;
    margin: 0;
    font-size: 13px;
    line-height: 1.55;
  }

  .circle-link {
    width: 58px;
    height: 58px;
    border: 1px solid rgba(255, 255, 255, 0.65);
    border-radius: 50%;
    display: grid;
    place-items: center;
    font-size: 20px;
    transition:
      background 0.3s ease,
      color 0.3s ease;
  }

  .circle-link:hover {
    background: white;
    color: #1c1c1a;
  }

  /* O NÁS */

  .intro {
    display: grid;
    grid-template-columns: 1fr 3fr;
    gap: 5vw;
    padding: 160px 8vw 190px 4vw;
  }

  .section-number {
    font-size: 10px;
    letter-spacing: 0.1em;
  }

  .intro-content {
    display: grid;
    grid-template-columns: 1.3fr 0.7fr;
    gap: 8vw;
  }

  .intro-content h2 {
    grid-column: 1 / -1;
    margin: 70px 0 20px;
    font-size: clamp(46px, 6vw, 92px);
    line-height: 0.92;
    letter-spacing: -0.055em;
    font-weight: 400;
  }

  .intro-text {
    grid-column: 2;
    max-width: 390px;
  }

  .intro-text p {
    font-size: 14px;
    line-height: 1.7;
    margin-bottom: 28px;
  }

  .intro-text strong {
    font-weight: 600;
  }

  .text-link {
    display: inline-flex;
    gap: 22px;
    margin-top: 15px;
    padding-bottom: 8px;
    border-bottom: 1px solid #1c1c1a;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  /* SLUŽBY */

  .services {
    display: grid;
    grid-template-columns: 1fr 3fr;
    gap: 5vw;
    padding: 0 8vw 190px 4vw;
  }

  .services-content > .eyebrow {
    margin-bottom: 80px;
  }

  .service {
    display: grid;
    grid-template-columns: 50px 1.2fr 1fr;
    gap: 30px;
    padding: 30px 0;
    border-top: 1px solid #c9c6be;
  }

  .service:last-child {
    border-bottom: 1px solid #c9c6be;
  }

  .service > span {
    color: #8a8881;
    font-size: 10px;
  }

  .service h3 {
    margin: 0;
    font-size: 20px;
    font-weight: 400;
    letter-spacing: -0.02em;
  }

  .service p {
    max-width: 300px;
    margin: 0;
    color: #77756f;
    font-size: 12px;
    line-height: 1.6;
  }

  /* PROJECTS */

  .projects {
    padding: 0 4vw 170px;
  }

  .section-heading {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 75px;
  }

  .section-heading h2 {
    margin: 24px 0 0;
    font-size: clamp(55px, 7vw, 110px);
    line-height: 0.85;
    letter-spacing: -0.06em;
    font-weight: 400;
  }

  .project-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    column-gap: 3vw;
    row-gap: 100px;
  }

  .project-2 {
    margin-top: 180px;
  }

  .project-4 {
    margin-top: 80px;
  }

  .project-image {
    position: relative;
    aspect-ratio: 1.15 / 1;
    overflow: hidden;
    background: #ddd;
  }

  .project-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.8s cubic-bezier(0.2, 0.7, 0.2, 1);
  }

  .project:hover .project-image img {
    transform: scale(1.04);
  }

  .project-arrow {
    position: absolute;
    right: 20px;
    bottom: 20px;
    width: 48px;
    height: 48px;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: white;
    opacity: 0;
    transform: translateY(10px);
    transition:
      opacity 0.3s ease,
      transform 0.3s ease;
  }

  .project:hover .project-arrow {
    opacity: 1;
    transform: translateY(0);
  }

  .project-meta {
    display: flex;
    justify-content: space-between;
    padding-top: 18px;
  }

  .project-meta h3 {
    margin-bottom: 5px;
    font-size: 17px;
    font-weight: 400;
    letter-spacing: -0.02em;
  }

  .project-meta p,
  .project-meta > span {
    margin: 0;
    color: #77756f;
    font-size: 11px;
    letter-spacing: 0.04em;
  }

  .projects-footer {
    margin-top: 100px;
    text-align: center;
  }

  .outline-button {
    display: inline-flex;
    gap: 45px;
    padding: 17px 22px;
    border: 1px solid #bbb8b0;
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    transition:
      background 0.3s ease,
      color 0.3s ease;
  }

  .outline-button:hover {
    background: #1c1c1a;
    color: white;
  }

  /* STATEMENT */

  .statement {
    padding: 190px 4vw;
    background: #20211e;
    color: #e8e5de;
  }

  .statement h2 {
    max-width: 1200px;
    margin: 120px 0 0;
    font-size: clamp(48px, 7vw, 110px);
    line-height: 0.94;
    letter-spacing: -0.06em;
    font-weight: 400;
  }

  .statement h2 em {
    color: #9f9d95;
  }

  /* KONTAKT */

  .contact {
    min-height: 85vh;
    padding: 150px 4vw 45px;
    background: #d8d4ca;
    display: flex;
    flex-direction: column;
  }

  .contact h2 {
    margin: auto 0;
    font-size: clamp(70px, 11vw, 170px);
    line-height: 0.82;
    letter-spacing: -0.07em;
    font-weight: 400;
  }

  .contact-link {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: min(600px, 100%);
    padding-bottom: 14px;
    border-bottom: 1px solid #77756f;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(24px, 3vw, 42px);
  }

  .contact-link span {
    font-family: Arial, Helvetica, sans-serif;
    font-size: 20px;
  }

  .contact-bottom {
    display: flex;
    justify-content: space-between;
    margin-top: 100px;
    color: #66645e;
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  /* FOOTER */

  footer {
    display: flex;
    justify-content: space-between;
    padding: 30px 4vw;
    background: #d8d4ca;
    border-top: 1px solid #b8b4aa;
  }

  footer p,
  footer > a:last-child {
    margin: 0;
    font-size: 9px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  /* MOBILE */

  @media (max-width: 800px) {
    .header {
      padding: 22px 5vw;
    }

    nav {
      display: none;
    }

    .menu {
      display: flex;
      flex-direction: column;
      gap: 5px;
      padding: 5px 0;
    }

    .menu span {
      display: block;
      width: 25px;
      height: 1px;
      background: white;
    }

    .hero-content {
      padding: 130px 5vw 30px;
    }

    h1 {
      font-size: clamp(54px, 15vw, 100px);
    }

    .hero-bottom p {
      width: 220px;
    }

    .intro,
    .services {
      grid-template-columns: 1fr;
      padding: 110px 5vw;
    }

    .intro-content {
      display: block;
    }

    .intro-content h2 {
      margin: 70px 0;
      font-size: 52px;
    }

    .intro-text {
      margin-left: 20%;
    }

    .services {
      padding-top: 0;
    }

    .services-content > .eyebrow {
      margin-bottom: 50px;
    }

    .service {
      grid-template-columns: 35px 1fr;
      gap: 15px;
    }

    .service h3 {
      font-size: 17px;
    }

    .service p {
      grid-column: 2;
      margin-top: 5px;
    }

    .projects {
      padding: 0 5vw 110px;
    }

    .section-heading {
      margin-bottom: 50px;
    }

    .project-grid {
      grid-template-columns: 1fr;
      row-gap: 65px;
    }

    .project-2,
    .project-4 {
      margin-top: 0;
    }

    .project-image {
      aspect-ratio: 1 / 1.05;
    }

    .statement {
      padding: 120px 5vw;
    }

    .statement h2 {
      margin-top: 80px;
      font-size: 50px;
    }

    .contact {
      min-height: 75vh;
      padding: 110px 5vw 30px;
    }

    .contact h2 {
      font-size: 18vw;
    }

    .contact-bottom {
      margin-top: 70px;
    }

    footer {
      padding: 25px 5vw;
    }

    footer p {
      display: none;
    }
  }
</style>
```