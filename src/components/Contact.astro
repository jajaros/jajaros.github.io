<section class="contact" id="contact">
        <div class="contact-top">
          <p class="eyebrow">Kontakt</p>

          <span class="section-number">04</span>
        </div>

        <h2>
          Vaša predstava.<br />
          Náš návrh.
        </h2>

        <!-- MENÁ + FAKTURAČNÉ ÚDAJE -->
        <div class="contact-details">
          <div class="contact-person">
            <p class="contact-label">Ing. Architekt Jedna</p>

            <a href="tel:+421XXXXXXXXX">
              +421 XXX XXX XXX
            </a>
          </div>

          <div class="contact-person">
            <p class="contact-label">Ing. Architekt Dva</p>

            <a href="tel:+421XXXXXXXXX">
              +421 XXX XXX XXX
            </a>
          </div>

          <div class="billing-address">
            <p class="contact-label">Fakturačné údaje</p>

            <p>
              Ulica a číslo · PSČ Mesto
            </p>
          </div>
        </div>

        <!-- EMAIL -->
        <a
          href="mailto:hello@atelier.example"
          class="contact-email"
        >
          <span>hello@atelier.example</span>
          <span class="email-arrow">↗</span>
        </a>
</section>

<style>

  .contact {
    min-height: auto;
    padding: 140px 4vw 45px;
    background: #d8d4ca;
    display: flex;
    flex-direction: column;
  }

  .contact-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .contact h2 {
    margin: 120px 0 100px;
    max-width: 1200px;
    font-size: clamp(64px, 11vw, 150px);
    line-height: 0.84;
    letter-spacing: -0.07em;
    font-weight: 400;
  }

  .contact h2 em {
    color: #77756f;
  }

  /* MENÁ + ADRESA */

  .contact-details {
    display: grid;
    grid-template-columns: 1fr 1fr 1.5fr;
    gap: 5vw;
    width: 100%;
    padding-top: 25px;
    border-top: 1px solid #aaa69d;
  }

  .contact-person,
  .billing-address {
    min-width: 0;
  }

  .contact-label {
    margin: 0 0 14px;
    color: #66645e;
    font-size: 9px;
    line-height: 1.4;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .contact-person a {
    display: inline-block;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(18px, 1.7vw, 13px);
    line-height: 1.15;
    border-bottom: 1px solid transparent;
    transition: border-color 0.25s ease;
  }

  .contact-person a:hover {
    border-color: #1c1c1a;
  }

  .billing-address p:last-child {
    margin: 0;
    color: #1c1c1a;
    font-size: 13px;
    line-height: 1.65;
  }

  /* EMAIL */

  .contact-email {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    width: 100%;
    margin-top: 85px;
    padding-bottom: 18px;
    border-bottom: 1px solid #77756f;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(42px, 7vw, 80px);
    line-height: 0.9;
    letter-spacing: -0.05em;
  }

  .contact-email span:first-child {
    transition: opacity 0.25s ease;
  }

  .contact-email:hover span:first-child {
    opacity: 0.55;
  }

  .email-arrow {
    font-family: Arial, Helvetica, sans-serif;
    font-size: clamp(24px, 3vw, 38px);
    line-height: 1;
    letter-spacing: 0;
    transition: transform 0.3s ease;
  }

  .contact-email:hover .email-arrow {
    transform: translate(5px, -5px);
  }

  .contact-bottom {
    display: flex;
    justify-content: space-between;
    margin-top: 100px;
    color: #66645e;
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

</style>